/.env

# Local Claude Code settings and hookify rules -- machine-specific, not portable.
/.claude/

# MCP runtime/telemetry data -- local cache, not portable.
/.mcp-data/

# Node dependencies -- reinstall via `cd mcp-server && npm install`.
/mcp-server/node_modules/

# mattpocock/skills clone -- general-purpose engineering skills, not part of Kilroy's own
# tracked tree (see CLAUDE.md's "Agent skills" section). Own nested git repo; never commit it.
/skills/
