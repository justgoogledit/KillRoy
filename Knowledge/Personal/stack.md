# Stack

Tools, languages, services. Claude defaults to these unless told otherwise.

## Languages
- *(e.g. TypeScript, Python, Go)*

## Frameworks / runtimes
- *(fill in)*

## Infra
- *(e.g. AWS, Vercel, Cloudflare)*

## Services
- *(e.g. GitHub, Stripe, Postgres, Redis)*

## Repos (where to `cd` for Claude Code sessions)
- *(one entry per active repo, with path and brief description)*
- *Example: `~/repos/my-app` — TypeScript monorepo (frontend/, backend/, shared/). Has its own `CLAUDE.md`.*

### Layout convention
- *(if you have one — e.g. `~/repos/<name>/` for repos you write to, `~/repos/_reference/<name>/` for read-only)*

## Editor / dev environment
- *(OS, editor, shell, key CLIs you rely on)*

## Things you DON'T use (and why)
- *(fill in to prevent Claude from suggesting them)*
- *Example: "Don't suggest jQuery. We use React."*

> Update this when stack changes. `session-recap` should surface stack mentions automatically.
