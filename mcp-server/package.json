{
  "name": "kilroy-connectors",
  "version": "0.1.0",
  "private": true,
  "description": "Kilroy's typed MCP connector server -- one read-only tool per data source. AMR Hub first; Overmind, Master Tracker CSV, and Planner follow (tickets #8-#10).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
